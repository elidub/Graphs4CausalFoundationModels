In this folder the code in priors is used to sample and process data for training. 

The input to the code here is a priordata_processing config that, together with a prior_config is used to generate a dataloader.  